# Data / link / event-stream layer
pandas>=2.0
pyarrow>=14
numpy>=1.24

# Causal layer (config, estimator, stats) -- §0, §5, §8
pyyaml>=6.0
lightgbm>=4.0        # GBT nuisance models for cross-fitted AIPW
scikit-learn>=1.3    # cross-fitting, metrics (AUROC/AUPRC), calibration
scipy>=1.11          # statistical tests / distributions

# Embeddings (the one GPU stage) -- §3. Install on the GPU node only.
# torch                # RAD-DINO + Clinical-Longformer inference
# transformers         # microsoft/rad-dino, yikuan8/Clinical-Longformer
# pillow               # image resize 512->224
