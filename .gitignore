# --- Data: MIMIC is credentialed PHI -- NEVER commit to git ---
/Datasets/
/data/
# storage_root (if ever placed in-repo): results, embeddings, patient tables
/ctfm_storage/
*.parquet
*.csv
*.csv.gz
*.zip
wget-log*

# --- Python environment & caches ---
/.venv/
/.tmp/
/.pipcache/
__pycache__/
*.pyc

# --- SLURM jobs & run scripts (machine-specific, not part of the code) ---
/slurm/
*.sbatch
*.sh

# --- SLURM logs & scratch ---
/logs/

# --- Private docs, instructions & correspondence: repo is code-only ---
# Ignore the whole docs/ tree (instructions, proposals, legacy notes, PDFs) ...
/docs/
# ... and any stray markdown anywhere ...
*.md
# ... except the project README(s), committed separately.
!/README.md
!/ctfm-icu/README.md

# --- misc ---
.DS_Store
